default: &default
  dispatchers:
    - polling_interval: 5
      batch_size: 500
  workers:
    - queues: "*"
      threads: 2
      polling_interval: 3

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
