<article class="stat-card <%= local_assigns[:accent] %>">
  <span class="stat-icon"><%= icon %></span>
  <div>
    <p><%= label %></p>
    <strong><%= value %></strong>
    <% if local_assigns[:meta].present? %>
      <small><%= meta %></small>
    <% end %>
  </div>
</article>
