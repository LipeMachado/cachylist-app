<%= render layout: "shared/app_shell" do %>
  <div class="px-10 py-10 border-b border-[var(--line)]">
    <h1 class="text-xl font-extrabold tracking-[-.03em] m-0">Nova Mídia</h1>
    <p class="mt-3 text-sm leading-[1.7] text-[var(--muted)]">Cadastre qualquer anime, série, filme, livro ou jogo em uma única ficha.</p>
  </div>
  <%= render "form", media_item: @media_item %>
<% end %>
